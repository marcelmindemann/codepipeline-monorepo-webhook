branchRouting:
  route: prefix
  routes:
    - master: prod
    - dev: staging

pullRequests:
  route: prefix
  routing: test

project:
  serviceModel: nested
  prefixParentFolder: true
  prefixRepoName: false